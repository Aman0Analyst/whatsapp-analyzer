[project]
name = "whatsapp-analyzer"
version = "0.1.0"
description = "Analyze WhatsApp chat"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "emoji==2.15.0",
    "python-dateutil==2.9.0.post0",
]

[tool.uv]
package = false
